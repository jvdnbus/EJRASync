<views:DarkThemeWindow x:Class="EJRASync.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EJRASync.UI"
        xmlns:views="clr-namespace:EJRASync.UI.Views"
        xmlns:controls="clr-namespace:EJRASync.UI.Controls"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        xmlns:lib="clr-namespace:EJRASync.Lib;assembly=EJRASync.Lib"
        mc:Ignorable="d"
        Height="800" Width="700" MinHeight="700" MinWidth="700"
        KeyDown="MainWindow_KeyDown">
    <views:DarkThemeWindow.Resources>
        <!-- Value converters for UI state -->
        <local:InverseBooleanConverter x:Key="InverseBooleanConverter"/>
        <local:BooleanToOpacityConverter x:Key="BooleanToOpacityConverter"/>
        <local:DateTimeToStringConverter x:Key="DateTimeToStringConverter"/>
        <local:ContextMenuVisibilityConverter x:Key="ContextMenuVisibilityConverter"/>
        <local:OrBooleanToVisibilityConverter x:Key="OrBooleanToVisibilityConverter"/>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        
        <!-- Dark ScrollBar Styles -->
        <Style x:Key="DarkScrollBarThumb" TargetType="{x:Type Thumb}">
            <Setter Property="Background" Value="#3E3E42"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Thumb}">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="2"/>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#555"/>
                            </Trigger>
                            <Trigger Property="IsDragging" Value="True">
                                <Setter Property="Background" Value="#666"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DarkScrollBarVertical" TargetType="{x:Type ScrollBar}">
            <Setter Property="Background" Value="#252526"/>
            <Setter Property="Width" Value="5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ScrollBar}">
                        <Grid>
                            <Rectangle Fill="{TemplateBinding Background}"/>
                            <Track x:Name="PART_Track" IsDirectionReversed="True">
                                <Track.Thumb>
                                    <Thumb Style="{StaticResource DarkScrollBarThumb}"/>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DarkScrollBarHorizontal" TargetType="{x:Type ScrollBar}">
            <Setter Property="Background" Value="#252526"/>
            <Setter Property="Height" Value="5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ScrollBar}">
                        <Grid>
                            <Rectangle Fill="{TemplateBinding Background}"/>
                            <Track x:Name="PART_Track">
                                <Track.Thumb>
                                    <Thumb Style="{StaticResource DarkScrollBarThumb}"/>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DarkScrollViewer" TargetType="{x:Type ScrollViewer}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ScrollViewer}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <ScrollContentPresenter Grid.Column="0" Grid.Row="0"/>
                            <ScrollBar x:Name="PART_VerticalScrollBar" 
                                       Grid.Column="1" Grid.Row="0"
                                       Style="{StaticResource DarkScrollBarVertical}"
                                       Orientation="Vertical"
                                       Value="{TemplateBinding VerticalOffset}"
                                       Maximum="{TemplateBinding ScrollableHeight}"
                                       ViewportSize="{TemplateBinding ViewportHeight}"
                                       Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}"/>
                            <ScrollBar x:Name="PART_HorizontalScrollBar"
                                       Grid.Column="0" Grid.Row="1"
                                       Style="{StaticResource DarkScrollBarHorizontal}"
                                       Orientation="Horizontal"
                                       Value="{TemplateBinding HorizontalOffset}"
                                       Maximum="{TemplateBinding ScrollableWidth}"
                                       ViewportSize="{TemplateBinding ViewportWidth}"
                                       Visibility="{TemplateBinding ComputedHorizontalScrollBarVisibility}"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Context Menu Icon Style -->
        <Style x:Key="ContextMenuIconStyle" TargetType="Image">
            <Setter Property="Width" Value="16"/>
            <Setter Property="Height" Value="16"/>
        </Style>

        <!-- Context Menu Style -->
        <Style x:Key="{x:Type ContextMenu}" TargetType="{x:Type ContextMenu}">
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ContextMenu}">
                        <Border Background="#2D2D30" CornerRadius="3,3,3,3" BorderBrush="#3E3E42" BorderThickness="2" Opacity="0.96">
                            <StackPanel ClipToBounds="True" Orientation="Vertical" IsItemsHost="True" Margin="5,4,5,4"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>    

        <ControlTemplate x:Key="{x:Static MenuItem.TopLevelItemTemplateKey}" TargetType="{x:Type MenuItem}">
            <Border Name="Border" >
                <Grid>
                    <ContentPresenter Margin="6,3,6,3" ContentSource="Header" RecognizesAccessKey="True" />
                </Grid>
            </Border>
        </ControlTemplate>
        
        <!-- Define the context menu as a resource -->
        <ContextMenu x:Key="RemoteFileContextMenu"  Background="#2D2D30" BorderBrush="#3E3E42">
            <MenuItem Header="Tag as Active" 
                     Command="{Binding RemoteFiles.TagAsActiveCommand}"
                     CommandParameter="{Binding RemoteFiles.SelectedFile}"
                     Background="#2D2D30"
                     Foreground="#F0F0F0"
                     IsEnabled="{Binding HasWriteAccess}">
                <MenuItem.Icon>
                    <Image Source="/Assets/Icons/check-square.png" Style="{StaticResource ContextMenuIconStyle}"/>
                </MenuItem.Icon>
                <MenuItem.Style>
                    <Style TargetType="MenuItem">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <MultiDataTrigger>
                                <MultiDataTrigger.Conditions>
                                    <Condition Binding="{Binding RemoteFiles.SelectedFile.IsDirectory}" Value="True"/>
                                    <Condition Binding="{Binding RemoteFiles.SelectedFile.IsActive}" Value="False"/>
                                </MultiDataTrigger.Conditions>
                                <Setter Property="Visibility" Value="Visible"/>
                            </MultiDataTrigger>
                            <MultiDataTrigger>
                                <MultiDataTrigger.Conditions>
                                    <Condition Binding="{Binding RemoteFiles.SelectedFile.IsDirectory}" Value="True"/>
                                    <Condition Binding="{Binding RemoteFiles.SelectedFile.IsActive}" Value="{x:Null}"/>
                                </MultiDataTrigger.Conditions>
                                <Setter Property="Visibility" Value="Visible"/>
                            </MultiDataTrigger>
                        </Style.Triggers>
                    </Style>
                </MenuItem.Style>
            </MenuItem>
            <MenuItem Header="Tag as Inactive" 
                     Command="{Binding RemoteFiles.TagAsActiveCommand}"
                     CommandParameter="{Binding RemoteFiles.SelectedFile}"
                     Background="#2D2D30"
                     Foreground="#F0F0F0"
                     IsEnabled="{Binding HasWriteAccess}">
                <MenuItem.Icon>
                    <Image Source="/Assets/Icons/subtract-square.png" Style="{StaticResource ContextMenuIconStyle}"/>
                </MenuItem.Icon>
                <MenuItem.Style>
                    <Style TargetType="MenuItem">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <MultiDataTrigger>
                                <MultiDataTrigger.Conditions>
                                    <Condition Binding="{Binding RemoteFiles.SelectedFile.IsDirectory}" Value="True"/>
                                    <Condition Binding="{Binding RemoteFiles.SelectedFile.IsActive}" Value="True"/>
                                </MultiDataTrigger.Conditions>
                                <Setter Property="Visibility" Value="Visible"/>
                            </MultiDataTrigger>
                        </Style.Triggers>
                    </Style>
                </MenuItem.Style>
            </MenuItem>
            <Separator/>
            <MenuItem Header="Delete" 
                     Command="{Binding RemoteFiles.DeleteRemoteFileCommand}"
                     CommandParameter="{Binding RemoteFiles.SelectedFile}"
                     Background="#2D2D30"
                     Foreground="#F0F0F0"
                     IsEnabled="{Binding HasWriteAccess}">
                <MenuItem.Icon>
                    <Image Source="/Assets/Icons/delete.png" Style="{StaticResource ContextMenuIconStyle}"/>
                </MenuItem.Icon>
            </MenuItem>
        </ContextMenu>
        
        <!-- Modern Button Style -->
        <Style x:Key="ModernButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#2D2D30"/>
            <Setter Property="Foreground" Value="#F0F0F0"/>
            <Setter Property="BorderBrush" Value="#3E3E42"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="16,10"/>
            <Setter Property="FontWeight" Value="Normal"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"
                                            Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#3E3E42"/>
                                <Setter Property="BorderBrush" Value="#555"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#555"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#252526"/>
                                <Setter Property="Foreground" Value="#6A6A6A"/>
                                <Setter Property="BorderBrush" Value="#333"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Modern TextBox Style -->
        <Style x:Key="ModernTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="#1E1E1E"/>
            <Setter Property="Foreground" Value="#F0F0F0"/>
            <Setter Property="BorderBrush" Value="#3E3E42"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ScrollViewer x:Name="PART_ContentHost" 
                                        Focusable="False"
                                        HorizontalScrollBarVisibility="Hidden"
                                        VerticalScrollBarVisibility="Hidden"
                                        Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="BorderBrush" Value="#555"/>
                            </Trigger>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#007ACC"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </views:DarkThemeWindow.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Top bar -->
            <RowDefinition Height="Auto"/> <!-- Path navigation -->
            <RowDefinition Height="*"/>    <!-- File panes -->
            <RowDefinition Height="Auto"/> <!-- Action buttons -->
            <RowDefinition Height="Auto"/> <!-- Status bar -->
        </Grid.RowDefinitions>

        <!-- Top Bar -->
        <Border Grid.Row="0" Background="#2D2D30" BorderBrush="#3E3E42" BorderThickness="0,0,0,1">
            <Grid Margin="20,12">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                    <Button Command="{Binding SyncCommand}" 
                            Style="{StaticResource ModernButtonStyle}"
                            Margin="0,0,12,0">
                        <StackPanel Orientation="Horizontal">
                            <Image Source="/Assets/Icons/download-pc.png" Width="16" Height="16" Margin="0,0,8,0"/>
                            <TextBlock Text="Sync" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                    <Button Command="{Binding ScanChangesCommand}" 
                            Style="{StaticResource ModernButtonStyle}"
                            Margin="0,0,12,0"
                            IsEnabled="{Binding HasWriteAccess}"
                            Visibility="{Binding HasWriteAccess, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <StackPanel Orientation="Horizontal">
                            <Image Source="/Assets/Icons/search.png" Width="16" Height="16" Margin="0,0,8,0"/>
                            <TextBlock Text="Scan Changes" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                    <Button Command="{Binding UpdateAvailableCommand}"
                            Visibility="{Binding IsUpdateAvailable, Converter={StaticResource BooleanToVisibilityConverter}}"
                            Style="{StaticResource ModernButtonStyle}"
                            Background="#2D4A2D"
                            Foreground="#4CAF50"
                            BorderBrush="#3A5A3A"
                            Margin="0,0,12,0">
                        <Button.Content>
                            <StackPanel Orientation="Horizontal">
                                <Image Source="/Assets/Icons/download.png" Width="16" Height="16" Margin="0,0,8,0"/>
                                <TextBlock VerticalAlignment="Center">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Text" Value="Update Available"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsDownloadingUpdate}" Value="True">
                                                    <Setter Property="Text" Value="Downloading..."/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </StackPanel>
                        </Button.Content>
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="ButtonBorder"
                                        Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        CornerRadius="4">
                                    <ContentPresenter HorizontalAlignment="Center" 
                                        VerticalAlignment="Center"
                                        Margin="{TemplateBinding Padding}"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="ButtonBorder" Property="Background" Value="#4A6A4A"/>
                                        <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="#5A7A5A"/>
                                    </Trigger>
                                    <Trigger Property="IsPressed" Value="True">
                                        <Setter TargetName="ButtonBorder" Property="Background" Value="#1D3A1D"/>
                                    </Trigger>
                                    <Trigger Property="IsEnabled" Value="False">
                                        <Setter TargetName="ButtonBorder" Property="Background" Value="#252526"/>
                                        <Setter Property="Foreground" Value="#6A6A6A"/>
                                        <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="#333"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <Button Command="{Binding RebuildHashStoreCommand}"
                            IsEnabled="{Binding HasWriteAccess}"
                            Visibility="{Binding HasWriteAccess, Converter={StaticResource BooleanToVisibilityConverter}}"
                            Style="{StaticResource ModernButtonStyle}"
                            Margin="0,0,12,0"
                            ToolTip="Rebuild .zstd hash store from bucket metadata">
                        <StackPanel Orientation="Horizontal">
                            <Image Source="/Assets/Icons/hash.png" Width="16" Height="16" Margin="0,0,8,0"/>
                            <TextBlock Text="Rebuild .zstd" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                    <Button Command="{Binding OpenReleaseCommand}"
                            Style="{StaticResource ModernButtonStyle}"
                            Margin="0,0,12,0">
                        <StackPanel Orientation="Horizontal">
                            <Image Source="/Assets/Icons/archive.png" Width="16" Height="16" Margin="0,0,8,0"/>
                            <TextBlock Text="Archive" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                    <!-- User Profile / Login -->
                    <Grid VerticalAlignment="Center">
                        <!-- Login Button (when not logged in) -->
                        <Button Command="{Binding LoginCommand}">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource ModernButtonStyle}">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsLoggedIn}" Value="False">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                            <StackPanel Orientation="Horizontal">
                                <Image Source="/Assets/Icons/user.png" Width="16" Height="16" Margin="0,0,8,0"/>
                                <TextBlock Text="Login" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                        
                        <Button Command="{Binding LogoutCommand}" 
                                Visibility="{Binding IsLoggedIn, Converter={StaticResource BooleanToVisibilityConverter}}"
                                ToolTip="Logout">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource ModernButtonStyle}">
                                    <Setter Property="Padding" Value="16,4"/>
                                </Style>
                            </Button.Style>
                            <StackPanel Orientation="Horizontal">
                                <!-- Profile Image -->
                                <Border Width="24" Height="24" 
                                        CornerRadius="12" 
                                        Background="#555"
                                        Margin="0,0,8,0">
                                    <Grid>
                                        <!-- Default user icon when no profile image -->
                                        <Image Source="/Assets/Icons/user.png" 
                                               Width="14" Height="14"
                                               HorizontalAlignment="Center" 
                                               VerticalAlignment="Center"
                                               Opacity="0.7"/>
                                        <!-- Actual profile image when available -->
                                        <Image Source="{Binding UserProfileImageUrl}"
                                               Width="24" Height="24"
                                               Stretch="UniformToFill"
                                               HorizontalAlignment="Center" 
                                               VerticalAlignment="Center">
                                            <Image.Clip>
                                                <EllipseGeometry RadiusX="12" RadiusY="12" Center="12,12"/>
                                            </Image.Clip>
                                        </Image>
                                    </Grid>
                                </Border>
                                
                                <!-- User Info -->
                                <StackPanel VerticalAlignment="Center">
                                    <TextBlock Text="{Binding UserDisplayName}" 
                                               FontSize="13" 
                                               FontWeight="SemiBold"
                                               Foreground="#F0F0F0"
                                               Margin="0,0,0,1"/>
                                    <TextBlock Text="{Binding UserProviderName}" 
                                               FontSize="11" 
                                               Foreground="#AAAAAA"/>
                                </StackPanel>
                                
                                <Image Source="/Assets/Icons/logout.png" 
                                       Width="14" Height="14" 
                                       Opacity="0.7"
                                       Margin="8,0,0,0"/>
                            </StackPanel>
                        </Button>
                    </Grid>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Path Navigation Bars -->
        <Border Grid.Row="1" Background="#252526" BorderBrush="#3E3E42" BorderThickness="0,0,0,1">
            <Grid Margin="20,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="12">
                        <ColumnDefinition.Style>
                            <Style TargetType="ColumnDefinition">
                                <Setter Property="Width" Value="12"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding HasWriteAccess}" Value="False">
                                        <Setter Property="Width" Value="0"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </ColumnDefinition.Style>
                    </ColumnDefinition>
                    <ColumnDefinition Width="*">
                        <ColumnDefinition.Style>
                            <Style TargetType="ColumnDefinition">
                                <Setter Property="Width" Value="*"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding HasWriteAccess}" Value="False">
                                        <Setter Property="Width" Value="0"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </ColumnDefinition.Style>
                    </ColumnDefinition>
                </Grid.ColumnDefinitions>
                
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="8"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <!-- Labels -->
                <TextBlock Grid.Row="0" Grid.Column="0" Text="Local Directory" 
                           FontWeight="SemiBold" FontSize="13" Foreground="#CCCCCC" Margin="4,0" />
                <TextBlock Grid.Row="0" Grid.Column="2" Text="Remote Storage" 
                           FontWeight="SemiBold" FontSize="13" Foreground="#CCCCCC" Margin="4,0"
                           Visibility="{Binding HasWriteAccess, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                
                <!-- Local Path Navigation -->
                <Grid Grid.Row="2" Grid.Column="0">
                    <Grid.Style>
                        <Style TargetType="Grid">
                            <Setter Property="Grid.ColumnSpan" Value="1"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding HasWriteAccess}" Value="False">
                                    <Setter Property="Grid.ColumnSpan" Value="3"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Grid.Style>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="8"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="8"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="8"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Change AC Path Button -->
                    <Button Grid.Column="0" 
                            Command="{Binding ChangeAcPathCommand}"
                            Width="40" Height="40"
                            Background="#2D2D30"
                            BorderBrush="#3E3E42"
                            BorderThickness="1"
                            ToolTip="Change Assetto Corsa installation directory">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="ButtonBorder"
                                        Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        CornerRadius="4">
                                    <ContentPresenter HorizontalAlignment="Center" 
                                        VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="ButtonBorder" Property="Background" Value="#3E3E42"/>
                                        <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="#555"/>
                                    </Trigger>
                                    <Trigger Property="IsPressed" Value="True">
                                        <Setter TargetName="ButtonBorder" Property="Background" Value="#555"/>
                                    </Trigger>
                                    <Trigger Property="IsEnabled" Value="False">
                                        <Setter TargetName="ButtonBorder" Property="Background" Value="#252526"/>
                                        <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="#333"/>
                                        <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.5"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                        <Image Source="/Assets/Icons/folder.png" Width="20" Height="20"/>
                    </Button>

                    <!-- Local Up Directory Button -->
                    <Button Grid.Column="2" 
                            Command="{Binding LocalFiles.NavigateUpCommand}"
                            Width="40" Height="40"
                            Background="#2D2D30"
                            BorderBrush="#3E3E42"
                            BorderThickness="1"
                            ToolTip="Go up one directory">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="ButtonBorder"
                                        Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        CornerRadius="4">
                                    <ContentPresenter HorizontalAlignment="Center" 
                                                    VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="ButtonBorder" Property="Background" Value="#3E3E42"/>
                                        <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="#555"/>
                                    </Trigger>
                                    <Trigger Property="IsPressed" Value="True">
                                        <Setter TargetName="ButtonBorder" Property="Background" Value="#555"/>
                                    </Trigger>
                                    <Trigger Property="IsEnabled" Value="False">
                                        <Setter TargetName="ButtonBorder" Property="Background" Value="#252526"/>
                                        <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="#333"/>
                                        <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.5"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                        <Image Source="/Assets/Icons/arrow-up.png" Width="20" Height="20"/>
                    </Button>
                    
                    <!-- Local Refresh Button -->
                    <Button Grid.Column="4" 
                            Command="{Binding LocalFiles.RefreshCommand}"
                            Width="40" Height="40"
                            Background="#2D2D30"
                            BorderBrush="#3E3E42"
                            BorderThickness="1"
                            ToolTip="Refresh directory">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="ButtonBorder"
                                        Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        CornerRadius="4">
                                    <ContentPresenter HorizontalAlignment="Center" 
                                                    VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="ButtonBorder" Property="Background" Value="#3E3E42"/>
                                        <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="#555"/>
                                    </Trigger>
                                    <Trigger Property="IsPressed" Value="True">
                                        <Setter TargetName="ButtonBorder" Property="Background" Value="#555"/>
                                    </Trigger>
                                    <Trigger Property="IsEnabled" Value="False">
                                        <Setter TargetName="ButtonBorder" Property="Background" Value="#252526"/>
                                        <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="#333"/>
                                        <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.5"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                        <Image Source="/Assets/Icons/refresh.png" Width="20" Height="20"/>
                    </Button>
                    
                    <!-- Local Path TextBox -->
                    <TextBox Grid.Column="6" 
                             Text="{Binding NavigationContext.LocalCurrentPath, Mode=OneWay}" 
                             IsReadOnly="True" 
                             Style="{StaticResource ModernTextBoxStyle}"/>
                </Grid>
                
                <!-- Remote Path Navigation -->
                <Grid Grid.Row="2" Grid.Column="2" Visibility="{Binding HasWriteAccess, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="8"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="8"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <!-- Remote Up Directory Button -->
                    <Button Grid.Column="0" 
                            Command="{Binding RemoteFiles.NavigateUpCommand}"
                            Width="40" Height="40"
                            Background="#2D2D30"
                            BorderBrush="#3E3E42"
                            BorderThickness="1"
                            ToolTip="Go up one directory">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="ButtonBorder"
                                        Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        CornerRadius="4">
                                    <ContentPresenter HorizontalAlignment="Center" 
                                                    VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="ButtonBorder" Property="Background" Value="#3E3E42"/>
                                        <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="#555"/>
                                    </Trigger>
                                    <Trigger Property="IsPressed" Value="True">
                                        <Setter TargetName="ButtonBorder" Property="Background" Value="#555"/>
                                    </Trigger>
                                    <Trigger Property="IsEnabled" Value="False">
                                        <Setter TargetName="ButtonBorder" Property="Background" Value="#252526"/>
                                        <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="#333"/>
                                        <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.5"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                        <Image Source="/Assets/Icons/arrow-up.png" Width="20" Height="20"/>
                    </Button>
                    
                    <!-- Remote Refresh Button -->
                    <Button Grid.Column="2" 
                            Command="{Binding RemoteFiles.RefreshCommand}"
                            Width="40" Height="40"
                            Background="#2D2D30"
                            BorderBrush="#3E3E42"
                            BorderThickness="1"
                            ToolTip="Refresh directory">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="ButtonBorder"
                                        Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        CornerRadius="4">
                                    <ContentPresenter HorizontalAlignment="Center" 
                                                    VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="ButtonBorder" Property="Background" Value="#3E3E42"/>
                                        <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="#555"/>
                                    </Trigger>
                                    <Trigger Property="IsPressed" Value="True">
                                        <Setter TargetName="ButtonBorder" Property="Background" Value="#555"/>
                                    </Trigger>
                                    <Trigger Property="IsEnabled" Value="False">
                                        <Setter TargetName="ButtonBorder" Property="Background" Value="#252526"/>
                                        <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="#333"/>
                                        <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.5"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                        <Image Source="/Assets/Icons/refresh.png" Width="20" Height="20"/>
                    </Button>
                    
                    <!-- Remote Path TextBox -->
                    <TextBox Grid.Column="4" 
                             Text="{Binding RemoteFiles.CurrentPath, Mode=OneWay}" 
                             IsReadOnly="True" 
                             Style="{StaticResource ModernTextBoxStyle}"/>
                </Grid>
            </Grid>
        </Border>

        <!-- File Panes -->
        <Grid Grid.Row="2" Margin="20,16,20,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="8">
                    <ColumnDefinition.Style>
                        <Style TargetType="ColumnDefinition">
                            <Setter Property="Width" Value="8"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding HasWriteAccess}" Value="False">
                                    <Setter Property="Width" Value="0"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </ColumnDefinition.Style>
                </ColumnDefinition>
                <ColumnDefinition Width="*">
                    <ColumnDefinition.Style>
                        <Style TargetType="ColumnDefinition">
                            <Setter Property="Width" Value="*"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding HasWriteAccess}" Value="False">
                                    <Setter Property="Width" Value="0"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </ColumnDefinition.Style>
                </ColumnDefinition>
            </Grid.ColumnDefinitions>
            
            <!-- Local File List -->
            <Border Grid.Column="0" Background="#1E1E1E" BorderBrush="#3E3E42" BorderThickness="0,1,0,0" CornerRadius="5,5,0,0">
                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="Grid.ColumnSpan" Value="1"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding HasWriteAccess}" Value="False">
                                <Setter Property="Grid.ColumnSpan" Value="3"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <Grid>
                    <!-- File list -->
                    <DataGrid ItemsSource="{Binding LocalFiles.Files}"
                             SelectedItem="{Binding LocalFiles.SelectedFile}"
                             x:Name="LocalFilesDataGrid"
                             ContextMenuOpening="LocalFilesDataGrid_ContextMenuOpening"
                             AutoGenerateColumns="False"
                             GridLinesVisibility="None"
                             HeadersVisibility="Column"
                             SelectionMode="Extended"
                             AllowDrop="True"
                             CanUserAddRows="False"
                             CanUserDeleteRows="False"
                             CanUserReorderColumns="False"
                             CanUserResizeRows="False"
                             ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                             ScrollViewer.CanContentScroll="True"
                             VirtualizingPanel.IsVirtualizing="True"
                             VirtualizingPanel.VirtualizationMode="Recycling"
                             IsEnabled="{Binding LocalFiles.IsLoading, Converter={StaticResource InverseBooleanConverter}}"
                             Opacity="{Binding LocalFiles.IsLoading, Converter={StaticResource BooleanToOpacityConverter}}"
                             Background="#1E1E1E"
                             BorderThickness="0"
                             RowHeight="24"
                             FontSize="13"
                             Margin="0,0,0,4" ColumnHeaderHeight="34">
                        
                        <DataGrid.Resources>
                            <Style TargetType="ScrollViewer" BasedOn="{StaticResource DarkScrollViewer}"/>
                            <Style TargetType="ScrollBar" BasedOn="{StaticResource DarkScrollBarVertical}"/>
                        </DataGrid.Resources>
                        
                        <DataGrid.ColumnHeaderStyle>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Background" Value="#2D2D30"/>
                                <Setter Property="Foreground" Value="#F0F0F0"/>
                                <Setter Property="BorderBrush" Value="#3E3E42"/>
                                <Setter Property="BorderThickness" Value="0,0,0,1"/>
                                <Setter Property="Padding" Value="8,0"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                            </Style>
                        </DataGrid.ColumnHeaderStyle>

                        <DataGrid.CellStyle>
                            <Style TargetType="DataGridCell">
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="Foreground" Value="#F0F0F0"/>
                            </Style>
                        </DataGrid.CellStyle>

                        <DataGrid.RowStyle>
                            <Style TargetType="DataGridRow">
                                <Setter Property="Background" Value="#1E1E1E"/>
                                <Setter Property="BorderThickness" Value="1"/>
                                <Setter Property="BorderBrush" Value="Transparent"/>
                                <Style.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Background" Value="#094771"/>
                                        <Setter Property="BorderBrush" Value="#007ACC"/>
                                    </Trigger>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#2A2D2E"/>
                                    </Trigger>
                                    <MultiTrigger>
                                        <MultiTrigger.Conditions>
                                            <Condition Property="IsSelected" Value="True"/>
                                            <Condition Property="IsMouseOver" Value="True"/>
                                        </MultiTrigger.Conditions>
                                        <Setter Property="Background" Value="#1177BB"/>
                                        <Setter Property="BorderBrush" Value="#007ACC"/>
                                    </MultiTrigger>
                                </Style.Triggers>
                            </Style>
                        </DataGrid.RowStyle>

                        <DataGrid.ContextMenu>
                            <ContextMenu  Background="#2D2D30" BorderBrush="#3E3E42">
                                <MenuItem Header="Scan Changes" 
                                         Command="{Binding LocalFiles.ScanChangesSelectedCommand}"
                                         CommandParameter="{Binding LocalFiles.SelectedFile}"
                                         Background="#2D2D30"
                                         Foreground="#F0F0F0"
                                         IsEnabled="{Binding HasWriteAccess}">
                                    <MenuItem.Icon>
                                        <Image Source="/Assets/Icons/search.png" Style="{StaticResource ContextMenuIconStyle}"/>
                                    </MenuItem.Icon>
                                </MenuItem>
                                <Separator Background="#3E3E42"/>
                                <MenuItem Header="Compress &amp; upload" 
                                         Command="{Binding LocalFiles.CompressAndUploadCommand}"
                                         CommandParameter="{Binding LocalFiles.SelectedFile}"
                                         Background="#2D2D30"
                                         Foreground="#F0F0F0"
                                         IsEnabled="{Binding HasWriteAccess}">
                                    <MenuItem.Icon>
                                        <Image Source="/Assets/Icons/upload-box.png" Style="{StaticResource ContextMenuIconStyle}"/>
                                    </MenuItem.Icon>
                                </MenuItem>
                                <MenuItem Header="Raw upload" 
                                         Command="{Binding LocalFiles.RawUploadCommand}"
                                         CommandParameter="{Binding LocalFiles.SelectedFile}"
                                         Background="#2D2D30"
                                         Foreground="#F0F0F0"
                                         IsEnabled="{Binding HasWriteAccess}">
                                    <MenuItem.Icon>
                                        <Image Source="/Assets/Icons/upload-file.png" Style="{StaticResource ContextMenuIconStyle}"/>
                                    </MenuItem.Icon>
                                </MenuItem>
                                <Separator Background="#3E3E42"/>
                                <MenuItem Header="Show in Explorer" 
                                         Command="{Binding LocalFiles.ViewInExplorerCommand}"
                                         CommandParameter="{Binding LocalFiles.SelectedFile}"
                                         Background="#2D2D30"
                                         Foreground="#F0F0F0">
                                    <MenuItem.Icon>
                                        <Image Source="/Assets/Icons/folder.png" Style="{StaticResource ContextMenuIconStyle}"/>
                                    </MenuItem.Icon>
                                </MenuItem>
                            </ContextMenu>
                        </DataGrid.ContextMenu>

                        <DataGrid.InputBindings>
                            <MouseBinding Gesture="LeftDoubleClick" 
                                         Command="{Binding LocalFiles.NavigateToCommand}"
                                         CommandParameter="{Binding LocalFiles.SelectedFile}"/>
                        </DataGrid.InputBindings>

                        <DataGrid.Columns>
                            <DataGridTemplateColumn Width="*" IsReadOnly="True">
                                <DataGridTemplateColumn.HeaderTemplate>
                                    <DataTemplate>
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Grid.Column="0" Text="Name" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                            <Border Grid.Column="2" x:Name="LocalSearchBorder" Background="#1E1E1E" BorderBrush="#3E3E42" BorderThickness="1" CornerRadius="3"
                                                    Width="180" Height="26" VerticalAlignment="Center">
                                                <Grid>
                                                    <TextBox Text="{Binding DataContext.LocalFiles.SearchText, RelativeSource={RelativeSource AncestorType=DataGrid}, UpdateSourceTrigger=PropertyChanged}" 
                                                             x:Name="LocalSearchTextBox"
                                                             KeyDown="SearchTextBox_KeyDown"
                                                             Background="Transparent"
                                                             Foreground="#F0F0F0"
                                                             CaretBrush="#F0F0F0"
                                                             BorderThickness="0"
                                                             Margin="24,4,8,4"
                                                             FontSize="12"
                                                             VerticalAlignment="Center">
                                                        <TextBox.Style>
                                                            <Style TargetType="TextBox">
                                                                <Style.Triggers>
                                                                    <Trigger Property="IsFocused" Value="True">
                                                                        <Setter Property="Background" Value="Transparent"/>
                                                                    </Trigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </TextBox.Style>
                                                    </TextBox>
                                                    <Image Source="/Assets/Icons/search.png" 
                                                           Width="12" Height="12" 
                                                           Opacity="0.7"
                                                           HorizontalAlignment="Left" 
                                                           VerticalAlignment="Center"
                                                           Margin="6,0,0,0"
                                                           IsHitTestVisible="False"/>
                                                </Grid>
                                                <Border.Style>
                                                    <Style TargetType="Border">
                                                        <Setter Property="BorderBrush" Value="#3E3E42"/>
                                                        <Style.Triggers>
                                                            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                                                                <Setter Property="BorderBrush" Value="#007ACC"/>
                                                            </Trigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Border.Style>
                                            </Border>
                                        </Grid>
                                    </DataTemplate>
                                </DataGridTemplateColumn.HeaderTemplate>
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <controls:HighlightTextBlock SourceText="{Binding Name}" 
                                                                     HighlightText="{Binding DataContext.LocalFiles.SearchText, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                                     VerticalAlignment="Center" 
                                                                     Margin="8,0"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <DataGridTextColumn Header="Size" Binding="{Binding DisplaySize}" Width="80" IsReadOnly="True">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                        <Setter Property="Margin" Value="8,0"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>

                            <DataGridTextColumn Header="Modified" Binding="{Binding LastModified, Converter={StaticResource DateTimeToStringConverter}, ConverterParameter='yyyy-MM-dd HH:mm'}" Width="130" IsReadOnly="True">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                        <Setter Property="Margin" Value="8,0"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>

            <!-- Remote File List -->
            <Border Grid.Column="2" Background="#1E1E1E" BorderBrush="#3E3E42" BorderThickness="0,1,0,0" CornerRadius="5,5,0,0"
                    Visibility="{Binding HasWriteAccess, Converter={StaticResource BooleanToVisibilityConverter}}">
                <Grid>
                    <!-- File list -->
                    <DataGrid 
                             ItemsSource="{Binding RemoteFiles.Files}"
                             SelectedItem="{Binding RemoteFiles.SelectedFile}"
                             x:Name="RemoteFilesDataGrid"
                             ContextMenuOpening="RemoteFilesDataGrid_ContextMenuOpening"
                             AutoGenerateColumns="False"
                             GridLinesVisibility="None"
                             HeadersVisibility="Column"
                             SelectionMode="Extended"
                             AllowDrop="True"
                             CanUserAddRows="False"
                             CanUserDeleteRows="False"
                             CanUserReorderColumns="False"
                             CanUserResizeRows="False"
                             ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                             ScrollViewer.CanContentScroll="True"
                             VirtualizingPanel.IsVirtualizing="True"
                             VirtualizingPanel.VirtualizationMode="Recycling"
                             IsEnabled="{Binding RemoteFiles.IsLoading, Converter={StaticResource InverseBooleanConverter}}"
                             Opacity="{Binding RemoteFiles.IsLoading, Converter={StaticResource BooleanToOpacityConverter}}"
                             Background="#1E1E1E"
                             BorderThickness="0"
                             RowHeight="24"
                             FontSize="13"
                             Margin="0,0,0,4" ColumnHeaderHeight="34">
                        
                        <DataGrid.Resources>
                            <Style TargetType="ScrollViewer" BasedOn="{StaticResource DarkScrollViewer}"/>
                            <Style TargetType="ScrollBar" BasedOn="{StaticResource DarkScrollBarVertical}"/>
                        </DataGrid.Resources>
                        
                        <DataGrid.ColumnHeaderStyle>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Background" Value="#2D2D30"/>
                                <Setter Property="Foreground" Value="#F0F0F0"/>
                                <Setter Property="BorderBrush" Value="#3E3E42"/>
                                <Setter Property="BorderThickness" Value="0,0,0,1"/>
                                <Setter Property="Padding" Value="8,0"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                            </Style>
                        </DataGrid.ColumnHeaderStyle>
                        
                        <DataGrid.CellStyle>
                            <Style TargetType="DataGridCell">
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="Foreground" Value="#F0F0F0"/>
                            </Style>
                        </DataGrid.CellStyle>

                        <DataGrid.RowStyle>
                            <Style TargetType="DataGridRow">
                                <Setter Property="Background" Value="#1E1E1E"/>
                                <Setter Property="BorderThickness" Value="1"/>
                                <Setter Property="BorderBrush" Value="Transparent"/>
                                <Style.Triggers>
                                    <!-- Default state for all rows -->
                                    <DataTrigger Binding="{Binding IsActive}" Value="{x:Null}">
                                        <Setter Property="Background" Value="Transparent"/>
                                    </DataTrigger>
                                    
                                    <!-- Active items -->
                                    <DataTrigger Binding="{Binding IsActive}" Value="True">
                                        <Setter Property="Background" Value="#2D4A2D"/>
                                    </DataTrigger>
                                    
                                    <!-- Inactive items -->
                                    <DataTrigger Binding="{Binding IsActive}" Value="False">
                                        <Setter Property="Background" Value="#4A2D2D"/>
                                        <Setter Property="Opacity" Value="0.7"/>
                                    </DataTrigger>
                                    
                                    <!-- Pending changes - highest priority -->
                                    <DataTrigger Binding="{Binding IsPendingChange}" Value="True">
                                        <Setter Property="Background" Value="#4A3500"/>
                                    </DataTrigger>
                                    
                                    <!-- Selection - higher priority than IsActive -->
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Background" Value="#094771"/>
                                        <Setter Property="BorderBrush" Value="#007ACC"/>
                                        <Setter Property="Opacity" Value="1.0"/>
                                    </Trigger>
                                    
                                    <!-- Hover effect -->
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#2A2D2E"/>
                                        <Setter Property="Opacity" Value="1.0"/>
                                    </Trigger>
                                    
                                    <!-- Selected + Hover -->
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Background" Value="#1177BB"/>
                                        <Setter Property="BorderBrush" Value="#007ACC"/>
                                        <Setter Property="Opacity" Value="1.0"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </DataGrid.RowStyle>
                        
                        <DataGrid.ContextMenu>
                            <ContextMenu x:Name="RemoteFileContextMenuInGrid" Background="#2D2D30" BorderBrush="#3E3E42">
                            </ContextMenu>
                        </DataGrid.ContextMenu>

                        <DataGrid.InputBindings>
                            <MouseBinding Gesture="LeftDoubleClick" 
                                         Command="{Binding RemoteFiles.NavigateToCommand}"
                                         CommandParameter="{Binding RemoteFiles.SelectedFile}"/>
                        </DataGrid.InputBindings>

                        <DataGrid.Columns>
                            <DataGridTemplateColumn Width="*" IsReadOnly="True">
                                <DataGridTemplateColumn.HeaderTemplate>
                                    <DataTemplate>
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Grid.Column="0" Text="Name" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                            <Border Grid.Column="2" x:Name="RemoteSearchBorder" Background="#1E1E1E" BorderBrush="#3E3E42" BorderThickness="1" CornerRadius="3"
                                                    Width="180" Height="26" VerticalAlignment="Center">
                                                <Grid>
                                                    <TextBox Text="{Binding DataContext.RemoteFiles.SearchText, RelativeSource={RelativeSource AncestorType=DataGrid}, UpdateSourceTrigger=PropertyChanged}" 
                                                             x:Name="RemoteSearchTextBox"
                                                             KeyDown="SearchTextBox_KeyDown"
                                                             Background="Transparent"
                                                             Foreground="#F0F0F0"
                                                             CaretBrush="#F0F0F0"
                                                             BorderThickness="0"
                                                             Margin="24,4,8,4"
                                                             FontSize="12"
                                                             VerticalAlignment="Center">
                                                        <TextBox.Style>
                                                            <Style TargetType="TextBox">
                                                                <Style.Triggers>
                                                                    <Trigger Property="IsFocused" Value="True">
                                                                        <Setter Property="Background" Value="Transparent"/>
                                                                    </Trigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </TextBox.Style>
                                                    </TextBox>
                                                    <Image Source="/Assets/Icons/search.png" 
                                                           Width="12" Height="12" 
                                                           Opacity="0.7"
                                                           HorizontalAlignment="Left" 
                                                           VerticalAlignment="Center"
                                                           Margin="6,0,0,0"
                                                           IsHitTestVisible="False"/>
                                                </Grid>
                                                <Border.Style>
                                                    <Style TargetType="Border">
                                                        <Setter Property="BorderBrush" Value="#3E3E42"/>
                                                        <Style.Triggers>
                                                            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                                                                <Setter Property="BorderBrush" Value="#007ACC"/>
                                                            </Trigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Border.Style>
                                            </Border>
                                        </Grid>
                                    </DataTemplate>
                                </DataGridTemplateColumn.HeaderTemplate>
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <controls:HighlightTextBlock SourceText="{Binding Name}" 
                                                                     HighlightText="{Binding DataContext.RemoteFiles.SearchText, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                                     VerticalAlignment="Center" 
                                                                     Margin="8,0"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="110" IsReadOnly="True">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                        <Setter Property="Margin" Value="8,0"/>
                                        <Setter Property="FontStyle" Value="Italic"/>
                                        <Setter Property="Foreground" Value="#FFA500"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>

                            <DataGridTextColumn Header="Size" Binding="{Binding DisplaySize}" Width="80" IsReadOnly="True">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                        <Setter Property="Margin" Value="8,0"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            
                            <DataGridTemplateColumn Header="C" Width="30">
                                <DataGridTemplateColumn.HeaderStyle>
                                    <Style TargetType="DataGridColumnHeader">
                                        <Setter Property="Background" Value="#2D2D30"/>
                                        <Setter Property="Foreground" Value="#F0F0F0"/>
                                        <Setter Property="BorderBrush" Value="#3E3E42"/>
                                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                                        <Setter Property="Padding" Value="8,6"/>
                                        <Setter Property="FontWeight" Value="SemiBold"/>
                                        <Setter Property="ToolTip" Value="Compressed"/>
                                    </Style>
                                </DataGridTemplateColumn.HeaderStyle>
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="Bold" Margin="8,0">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsCompressed}" Value="True">
                                                            <Setter Property="Text" Value="✓"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding IsCompressed}" Value="False">
                                                            <Setter Property="Text" Value=""/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            
                            <DataGridTextColumn Header="Modified" Binding="{Binding LastModified, Converter={StaticResource DateTimeToStringConverter}, ConverterParameter='yyyy-MM-dd HH:mm'}" Width="130" IsReadOnly="True">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                        <Setter Property="Margin" Value="8,0"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>
        </Grid>

        <!-- Action Buttons -->
        <Border Grid.Row="3" Background="#252526" BorderBrush="#3E3E42" BorderThickness="0,1,0,0" 
                IsEnabled="{Binding HasWriteAccess}"
                Visibility="{Binding HasWriteAccess, Converter={StaticResource BooleanToVisibilityConverter}}">
            <StackPanel Orientation="Horizontal" 
                       HorizontalAlignment="Right" 
                       Margin="20,16">
                <Button Command="{Binding ViewChangesCommand}" 
                        Style="{StaticResource ModernButtonStyle}"
                        Margin="0,0,12,0">
                    <StackPanel Orientation="Horizontal">
                        <Image Source="/Assets/Icons/bullet-list.png" Width="16" Height="16" Margin="0,0,8,0"/>
                        <TextBlock Text="{Binding ViewChangesButtonText}" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
                <Button Command="{Binding DiscardChangesCommand}" 
                        Background="#4A2D2D"
                        Foreground="#FF6B6B"
                        BorderBrush="#6B3A3A"
                        BorderThickness="1"
                        Padding="16,10"
                        FontWeight="Normal"
                        FontSize="13"
                        Margin="0,0,12,0">
                    <Button.Content>
                        <StackPanel Orientation="Horizontal">
                            <Image Source="/Assets/Icons/recycle.png" Width="16" Height="16" Margin="0,0,8,0"/>
                            <TextBlock Text="Discard" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button.Content>
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="ButtonBorder"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="4">
                                <ContentPresenter HorizontalAlignment="Center" 
                                                VerticalAlignment="Center"
                                                Margin="{TemplateBinding Padding}"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="ButtonBorder" Property="Background" Value="#7B4A4A"/>
                                    <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="#9B5A5A"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="ButtonBorder" Property="Background" Value="#5B2A2A"/>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter TargetName="ButtonBorder" Property="Background" Value="#252526"/>
                                    <Setter Property="Foreground" Value="#6A6A6A"/>
                                    <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="#333"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
                <Button Command="{Binding ApplyChangesCommand}" 
                        Background="#2D4A2D"
                        Foreground="#4CAF50"
                        BorderBrush="#3A5A3A"
                        BorderThickness="1"
                        Padding="16,10"
                        FontWeight="Normal"
                        FontSize="13">
                    <Button.Content>
                        <StackPanel Orientation="Horizontal">
                            <Image Source="/Assets/Icons/check.png" Width="16" Height="16" Margin="0,0,8,0"/>
                            <TextBlock Text="Apply Changes" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button.Content>
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="ButtonBorder"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="4">
                                <ContentPresenter HorizontalAlignment="Center" 
                                                VerticalAlignment="Center"
                                                Margin="{TemplateBinding Padding}"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="ButtonBorder" Property="Background" Value="#4A6A4A"/>
                                    <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="#5A7A5A"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="ButtonBorder" Property="Background" Value="#1D3A1D"/>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter TargetName="ButtonBorder" Property="Background" Value="#252526"/>
                                    <Setter Property="Foreground" Value="#6A6A6A"/>
                                    <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="#333"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </StackPanel>
        </Border>

        <!-- Status Bar -->
        <Border Grid.Row="4" Background="#252526" BorderBrush="#3E3E42" BorderThickness="0,1,0,0">
            <Grid Height="36" Margin="20,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="200"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock Grid.Column="0" 
                          Text="{Binding StatusMessage}" 
                          VerticalAlignment="Center"
                          FontSize="13"
                          Foreground="#F0F0F0"/>

                <Button Grid.Column="1" 
                        Command="{Binding CancelOperationCommand}"
                        Background="#3E3E42"
                        Foreground="#CCCCCC"
                        BorderBrush="#555555"
                        BorderThickness="1"
                        Width="24"
                        Height="24"
                        Margin="8,0,8,0"
                        ToolTip="Cancel operation">
                    <Button.Visibility>
                        <MultiBinding Converter="{StaticResource OrBooleanToVisibilityConverter}">
                            <Binding Path="IsScanning"/>
                            <Binding Path="IsApplying"/>
                            <Binding Path="IsSyncing"/>
                        </MultiBinding>
                    </Button.Visibility>
                    <Image Source="/Assets/Icons/delete.png" Width="12" Height="12"/>
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="ButtonBorder"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="12">
                                <ContentPresenter HorizontalAlignment="Center" 
                                                VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="ButtonBorder" Property="Background" Value="#555555"/>
                                    <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="#777777"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="ButtonBorder" Property="Background" Value="#2A2A2A"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>

                <ProgressBar Grid.Column="2" 
                            Value="{Binding ProgressValue}" 
                            Maximum="100" 
                            Height="6" 
                            VerticalAlignment="Center"
                            Background="#3E3E42"
                            Foreground="#F0F0F0"
                            BorderThickness="0"
                            Visibility="Visible"
                            IsIndeterminate="{Binding IsProgressIndeterminate}">
                    <ProgressBar.Template>
                        <ControlTemplate TargetType="ProgressBar">
                            <Border Background="{TemplateBinding Background}" 
                                    CornerRadius="3"
                                    ClipToBounds="True">
                                <Grid>
                                    <Rectangle Name="PART_Track" />
                                    <Grid Name="PART_Indicator" 
                                          ClipToBounds="True"
                                          HorizontalAlignment="Left">
                                        <Rectangle Name="Indicator" 
                                                   Fill="{TemplateBinding Foreground}"
                                                   RadiusX="3" RadiusY="3"/>
                                        <Rectangle Name="IndeterminateIndicator" 
                                                   Fill="{TemplateBinding Foreground}"
                                                   RadiusX="3" RadiusY="3"
                                                   Width="30"
                                                   HorizontalAlignment="Left"
                                                   Visibility="Collapsed">
                                            <Rectangle.RenderTransform>
                                                <TranslateTransform x:Name="IndeterminateTransform"/>
                                            </Rectangle.RenderTransform>
                                        </Rectangle>
                                    </Grid>
                                </Grid>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsIndeterminate" Value="True">
                                    <Setter TargetName="Indicator" Property="Visibility" Value="Collapsed"/>
                                    <Setter TargetName="IndeterminateIndicator" Property="Visibility" Value="Visible"/>
                                    <Trigger.EnterActions>
                                        <BeginStoryboard>
                                            <Storyboard RepeatBehavior="Forever">
                                                <DoubleAnimation Storyboard.TargetName="IndeterminateTransform"
                                                               Storyboard.TargetProperty="X"
                                                               From="-30"
                                                               To="200"
                                                               Duration="0:0:1.5"
                                                               AutoReverse="False"/>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </Trigger.EnterActions>
                                    <Trigger.ExitActions>
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <DoubleAnimation Storyboard.TargetName="IndeterminateTransform"
                                                               Storyboard.TargetProperty="X"
                                                               To="0"
                                                               Duration="0:0:0"/>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </Trigger.ExitActions>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </ProgressBar.Template>
                </ProgressBar>
            </Grid>
        </Border>
    </Grid>
</views:DarkThemeWindow>
